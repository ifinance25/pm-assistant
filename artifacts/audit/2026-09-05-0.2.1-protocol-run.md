# Прогон протокола: релиз 0.2.1

**Дата:** 2026-09-05  
**Версия:** 0.2.1 (`app/package.json`)  
**Хост:** EC2 `16.171.52.89`  
**Протокол:** [2026-09-05-composer-2.5-verification-protocol.md](2026-09-05-composer-2.5-verification-protocol.md)

Локально: `cd app && npm test` (tsc + vitest) **185/185**, `npm run lint` pass, `npm run build` pass.  
Прод: `sync.sh` → `/opt/pm-assistant/`, `npm install` + `npm rebuild better-sqlite3` + `npm run build`, restart `pm-assistant-api` / `pm-assistant-worker`, `doctor.sh` OK.

Секреты не печатались. Пароль только из `/etc/pm-assistant.env` на сервере.

---

## 0. Предусловия

| ID | Команда / действие | Статус |
|----|--------------------|--------|
| PRE-01 | `cd app && npm test` | **pass** 185/185 (44 файла) |
| PRE-02 | `npx tsc --noEmit` (внутри `npm test`) | **pass** 0 ошибок |
| PRE-03 | `cd app && npm run build` | **pass** Vite dist |
| PRE-04 | `bash /opt/pm-assistant/deploy/doctor.sh` | **pass** «doctor: всё ок» |
| PRE-05 | `GET /api/health` | **pass** `workerAlive: true` |

## 1. P0

| ID | Статус | Доказательство |
|----|--------|----------------|
| SEC-01 | **pass** (тест) / **blocked** live Google | `audit-fixes.test.ts`: callback stranger → `error=forbidden`, пользователь не создан. На EC2 `GOOGLE_CLIENT_*` нет, `/api/auth/google/start` = 503 |
| SEC-02 | **pass** | тест: callback без state → `error=state`, cookie сессии нет; start кладёт `pm_assistant_oauth_state` |
| SEC-03 | **pass** | тест: `email_verified=false` → `error=unverified` |
| SEC-04 | **pass** | тест: 11-й логин несуществующего email → 429 |
| SEC-05 | **pass** | прод Set-Cookie: HttpOnly, SameSite=Lax; Secure нет (HTTP localhost, ожидаемо) |
| ISO-01 | **pass** | тест: B не видит встречу A; прод: один аккаунт |
| ISO-02 | **pass** | тест: GET чужой встречи → 404 |
| ISO-03 | **pass** | тест: PUT settings от role=user → 403 |
| PLT-01 | **pass** | прод POST Meet → 503 «не реализован», в БД не пишется |
| PLT-02 | **pass** | прод POST Телемост → 503 |
| PLT-03 | **pass** | `MeetingView.test.tsx`: баннер «демо-данные» |
| PLT-04 | **blocked** | живой Zoom с `ZOOM_CLIENT_*` в этой сессии не запускали |
| TRK-01 | **pass** | прод `GET /api/integrations/tracker/clickup/start`: `connected=false`, `stub=true` |
| TRK-02 | **pass** | тест: stub dispatch → `queued`, не `created` |
| TRK-03 | **blocked** | нет живого ClickUp PAT/API |
| TRK-04 | **pass** | тест повторного `migrate()`; прод `PRAGMA user_version=2` после бэкфилла admin |
| QA-01 | **pass** | `"test": "tsc --noEmit && vitest run"` |
| QA-02 | **pass** | `"lint": "biome check …"`, exit 0 |

## 2. P1

| ID | Статус | Доказательство |
|----|--------|----------------|
| SET-01 | **pass** | прод PUT `trackerType=invalid` → 400 (после бэкфилла admin) |
| SET-02 | **pass** | прод PUT webhook `169.254.169.254` → 400 |
| SET-03 | **pass** | прод PUT `{` → 400 |
| SRCH-01 | **pass** (тест) / **skip** прод | unit: известная фраза находится; прод `q=бюджет` → 0 хитов (корпус) |
| SRCH-02 | **pass** | прод `q="` → 200 `{"results":[]}` |
| SRCH-03 | **pass** | прод `q=OR` → 200 `{"results":[]}` |
| WRK-01 | **pass** | тест: таймаут LLM → error, следующая встреча ready |
| WRK-02 | **pass** | `recover.test.ts` |
| WRK-03 | **pass** | `meeting-detail.test.ts`: retry/transcribe при wav |
| WRK-04 | **pass** | прод POST `{}` от admin → 400 «нужно подтверждение»; тест user → 403 |
| ASN-01 | **pass** | тест: 2-я задача 500 → только первая `created`/`sent` |
| PRJ-01 | **pass** | тест: delete default → createMeeting создаёт проект |
| PRJ-02 | **pass** | прод PUT batch `name=""` → 400 |

## 3. UI v2

| ID | Статус | Доказательство |
|----|--------|----------------|
| UI-01 | **pass** (password) / **blocked** Google UI | `LoginView.test.tsx`; прод login 200. Google на EC2 без client → 503 |
| UI-02 | **pass** | `/register`, `/forgot-password`: «фаза 1.5» в `App.tsx` |
| UI-03 | **pass** | `Shell.test.tsx`: свёртка, аватар, «Выйти» |
| UI-04 | **pass** | `HomeView.test.tsx`: `home__stepper` |
| UI-05 | **pass** | API и `Home.tsx`: «выберите проект» |
| UI-06 | **pass** | `MeetingView.test.tsx`: одна колонка |
| UI-07 | **pass** | прод `PUT /api/projects/batch` 200 (create «QA 0.2.1 smoke») |
| UI-08 | **pass** | `SettingsView.test.tsx`: Asana, Trello, ClickUp, Notion |
| UI-09 | **pass** | dist на EC2 содержит `0.2.1` |
| UI-10 | **pass** | `ArchiveView.test.tsx` + `projectId` |
| UI-11 | **pass** | `CalendarView`: «Google Календарь: не подключён», CTA `/settings#integrations` |

## 4. Живой звонок

| ID | Статус | Доказательство |
|----|--------|----------------|
| LIVE-01 | **blocked** | новый Zoom 15+ мин не проводили |
| LIVE-02 | **blocked** | нет нового эталона STT в этой сессии |
| LIVE-03 | **blocked** | зависит от LIVE-01 |
| LIVE-04 | **pass** | `pipeline.test.ts`: 429 revise → ready + notice |
| LIVE-05 | **pass** | прод `Range: bytes=0-1023` → **206** на ready-встрече со звуком |

## 5. Деплой

| ID | Статус | Доказательство |
|----|--------|----------------|
| DEP-01 | **pass** с оговоркой | На сервере `http://127.0.0.1:5173/login` = 200. На Mac `:5173` занят локальным Vite; туннель `127.0.0.1:15173` → 200 |
| DEP-02 | **pass** | FT-01…08: health, login, 401, settings, POST zoom 201, UI, workerAlive, версия 0.2.1 |
| DEP-03 | **pass** | `/opt/pm-assistant/CHANGELOG.md` секция `[0.2.1]` |
| DEP-04 | **pass** с оговоркой | Ключи не в git. В `/etc/pm-assistant.env` **нет** `GOOGLE_CLIENT_*` / `ASANA_PAT` (filled=false). doctor не ругается |

## 6. Supabase (eval)

| ID | Статус | Комментарий |
|----|--------|-------------|
| SB-01 | **skip** | PoC не делали; локальный allowlist закрывает P0-01 |
| SB-02 | **pass** | Оценка уже в code-review: 3-5 дней, async, не чинит P0 |
| SB-03 | **pass** | Pulse Decisions Log: Postgres отложить |

---

## Инцидент на проде (закрыт)

После выкладки колонка `users.role` получила DEFAULT `user`. Единственная учётка env стала не-admin: PUT settings и bot restart → 403.  
Исправление: однократный бэкфилл `PRAGMA user_version=2` + `UPDATE users SET role='admin'`; env-email снова повышается при старте. Повторный smoke SET-01…03 и WRK-04: **pass**.
