import { PlaceholderPage } from "../PlaceholderPage.tsx";

export function IntegrationsPlaceholder() {
  return (
    <PlaceholderPage
      title="Интеграции"
      text="Подключение Zoom, Meet и Телемоста появится позже. MCP: npm run mcp (stdio), как подключить: README."
    />
  );
}
